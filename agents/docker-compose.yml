version: "3.9"

services:
  web:
    build: .
    container_name: crewai_pdf_processor
    ports:
      - "5000:5000"
    env_file: 
      - .env
    restart: unless-stopped
    volumes:
      - .:/app
    deploy:
      resources:
        limits:
          memory: 1g
